# Environment Type
# Available options: development, test, production
NODE_ENV=development
NEXT_PUBLIC_ENV=development

# API Configuration
NEXT_PUBLIC_API_URL=https://api.dev.example.com

# Feature Flags
# Set to 'true' ONLY in test environment to use mock data instead of real API calls
# Should be 'false' in development and production
NEXT_PUBLIC_USE_MOCKS=false

# Logging
# This is automatically set based on environment, but can be overridden
# Available options: debug, info, warn, error
# LOG_LEVEL=debug 